apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trabajo-k8s-fe.fullname" . }}-test-connection"
  labels:
    {{- include "trabajo-k8s-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trabajo-k8s-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
