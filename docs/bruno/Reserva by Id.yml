info:
  name: Reserva by Id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/reservations/254
  headers:
    - name: Authorization
      value: bearer token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
