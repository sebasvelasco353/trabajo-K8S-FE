info:
  name: get by user id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/reservations/byUser/qgwrgwegewg
  headers:
    - name: Authorization
      value: bearer ifwfi
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
